# DISABLED: Duplicate scheduling - Netlify scheduler is handling news collection
# To re-enable, uncomment the 'on:' section below
name: Medical News Collection (DISABLED)

on:
  workflow_dispatch: # Allow manual trigger only (disabled schedule)

jobs:
  collect-news:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger News Collection
        run: |
          curl -X POST "$SUPABASE_URL/functions/v1/news-collection" \
            -H "Authorization: Bearer ${{ secrets.SUPABASE_ANON_KEY }}" \
            -H "Content-Type: application/json" \
            -d '{"force": true}' \
            --fail-with-body
        env:
          SUPABASE_URL: https://kvsqtolsjggpyvdtdpss.supabase.co